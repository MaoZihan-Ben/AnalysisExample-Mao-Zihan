Dear sir or madam
If you wish to review the entire code or the running results, please refer to: MAO's analytical code summary.py. 
The other branches are the analysis sections and cannot be directly run：Lack of necessary libraries. 
They are only for the  viewing.
If you want to run it directly, you can insert it yourself:
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
import os
import statsmodels.api as sm
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import roc_auc_score, r2_score, mean_squared_error, mean_absolute_error
from sklearn.ensemble import RandomForestRegressor
from sklearn.model_selection import train_test_split
from sklearn.neural_network import MLPRegressor
from sklearn.preprocessing import StandardScaler
import warnings

Repectfully
Mao Zihan
